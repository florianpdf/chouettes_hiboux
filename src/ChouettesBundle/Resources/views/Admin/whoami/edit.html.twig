{% extends 'base.html.twig' %}

{% block header %}
    {% include '@Chouettes/Default/navbar.html.twig' %}
{% endblock %}

{% block body %}
<div class="container admin">
    <h1 class="whoadmin">Modifier "Qui suis-je?"t</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <button type="submit" title="valider"><i class="material-icons accessoires">done</i></button>
    {{ form_end(edit_form) }}

    <div class="boutons">
        <div class="row">
            <div class="col offset-l4 l2">
                <a href="{{ path('whoami_index') }}" title="Revenir à l'accueil"><i class="material-icons accessoires">home</i></a>
            </div>
            <div class="col l2">
            {{ form_start(delete_form) }}
                <button type="submit" title="Effacer"><i class="material-icons accessoires">delete</i></button>
            {{ form_end(delete_form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
