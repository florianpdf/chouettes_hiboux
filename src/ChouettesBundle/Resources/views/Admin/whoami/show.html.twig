{% extends 'base.html.twig' %}

{% block body %}
    <h1>Whoami</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ whoami.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ whoami.text }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('whoami_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('whoami_edit', { 'id': whoami.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
