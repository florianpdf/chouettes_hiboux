{% extends 'base.html.twig' %}

{% block header %}
    {% include '@Chouettes/Default/navbar.html.twig' %}
{% endblock %}

{% block body %}

    <div class="container admin">

<h1 class="citationadmin">Liste des citations</h1>

    <table>
        <thead>
            <tr>
                <th>Texte</th>
                <th>Editer</th>
            </tr>
        </thead>
        <tbody>
        {% for citation in citations %}
            <tr>
                <td>{{ citation.text }}</td>
                <td>
                    <div class="boutons">
                            <a href="{{ path('citation_edit', { 'id': citation.id }) }}" title="Editer"><i class="material-icons doudous">edit</i></a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="boutons">
            <a href="{{ path('citation_new') }}" title="Créer une nouvelle citation"><i class="material-icons doudous">add_circle</i></a>
    </div>
        </div>
{% endblock %}
