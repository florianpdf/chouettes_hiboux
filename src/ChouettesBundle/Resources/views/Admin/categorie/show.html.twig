{% extends 'base.html.twig' %}

{% block header %}
    {% include '@Chouettes/Default/navbar.html.twig' %}
{% endblock %}

{% block body %}
    <div class="container admin">
    <h1>Catégorie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <div class="boutons">
        <div class="row">
            <div class="col offset-l3 l2">
            <a class="waves-effect waves-light btn" href="{{ path('categorie_index') }}">Retour</a>
                <a href="{{ path('admin_homepage') }}" title="Retourner à l'accueil de l'administration"><i class="material-icons">dashboard</i></a>

            </div>
            <div class="col l2">
            <a class="waves-effect waves-light btn" href="{{ path('categorie_edit', { 'id': categorie.id }) }}">Editer</a>
            </div>
            <div class="col l2">
                {{ form_start(delete_form) }}<input class="waves-effect waves-light btn" type="submit" value="Effacer">{{ form_end(delete_form) }}
            </div>
    </div>
        </div>
    </div>
{% endblock %}
