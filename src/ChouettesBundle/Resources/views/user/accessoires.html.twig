{% extends 'base.html.twig' %}

{% block title %}
    Accessoires -
{% endblock %}

{% block body %}

    <div class="main">
        <ul id="og-grid" class="og-grid">
            {% for modele in modeles %}
                <li>
                    <a href="#" data-largesrc="{{ asset('uploads/images/' ~ modele.imageUrl) }}" class="imglg" data-title="{{ modele.titre }}" data-description="{{ modele.contenu|raw }}">
                        <img src="{{ modele.imageUrl | imagine_filter('my_thumb') }}" class="thumbs" alt="{{ modele.imageAlt }}"/>
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>

    <div class="navigation center">
        {{ knp_pagination_render(modeles) }}
    </div>

{% endblock %}
